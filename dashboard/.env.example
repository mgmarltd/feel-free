# Calmutopia API base URL.
# Defaults to the production server if unset (see src/lib/api.ts).
# For local development against a server running on this machine:
# VITE_API_BASE=http://localhost:3900
VITE_API_BASE=http://91.99.194.149:3900
